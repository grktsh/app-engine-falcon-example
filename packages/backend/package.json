{
  "private": true,
  "name": "backend",
  "version": "0.1.0",
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "linters": {
      "*.py": [
        "pipenv run isort --atomic",
        "ffffff",
        "pipenv run flake8",
        "mypy",
        "git add"
      ]
    },
    "ignore": [
      "**/tests/appengine.py"
    ]
  },
  "devDependencies": {
    "lint-staged": "^7.2.0"
  }
}
